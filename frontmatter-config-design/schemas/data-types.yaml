string:
  extends: root             # Inherits attributes from root type
  placeholder_text: string  # Placeholder text for input fields
  pattern: string           # Regex pattern for validation
  min_length: number        # Minimum length of the string
  max_length: number        # Maximum length of the string

number:
  extends: root             # Inherits attributes from root type
  placeholder_text: string  # Placeholder text for input fields
  minimum: number           # Minimum value
  maximum: number           # Maximum value
  integer: boolean         # Whether the number should be an integer

boolean:
  extends: root             # Inherits attributes from root type

array:
  extends: root             # Inherits attributes from root type
  items_type: string        # Type of items in the array
  items: array              # Type of items in the array
  allowMultiple: boolean    # Whether multiple items are allowed
  min_items: number         # Minimum number of items